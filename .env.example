PORT=3000

# MySQL
DB_HOST=8.138.132.154
DB_PORT=3306
DB_USERNAME=daily_plan_db
DB_PASSWORD=4c3370dd0d
DB_DATABASE=daily_plan_db

# 开发环境下自动建表，生产环境务必关闭并使用迁移
# DB_SYNCHRONIZE=true

# JWT（双 token：access 短期 + refresh 长期，两个密钥务必不同）
JWT_SECRET=change-me-in-production
JWT_EXPIRES_IN=15m
JWT_REFRESH_SECRET=change-me-refresh-in-production
JWT_REFRESH_EXPIRES_IN=7d

# 本地图片上传
UPLOAD_DIR=uploads
UPLOAD_MAX_SIZE_MB=10

# 邀请链接
INVITE_BASE_URL=http://localhost:3000/invite
